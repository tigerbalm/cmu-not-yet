buildscript {
    repositories {
        jcenter()
    }
    dependencies {
    }
}

allprojects {
    repositories {
        jcenter()
        maven { url 'https://repo.eclipse.org/content/repositories/paho-releases/' }
    }
}
